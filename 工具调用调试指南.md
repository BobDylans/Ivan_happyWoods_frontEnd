# 工具调用调试指南

## 📋 API 调用对比

### ✅ HTML 示例（已验证可用）

```javascript
const response = await fetch(`${apiUrl}/api/v1/chat/?t=${Date.now()}`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'X-API-Key': apiKey
    },
    body: JSON.stringify({
        message: message,
        session_id: SESSION_ID,
        user_id: 'demo_user',
        stream: true,
        model_config: {
            max_tokens: 8000
        }
    })
});
```

### ✅ React 实现（已确认匹配）

位置：`client/src/hooks/use-sse-stream.ts`

```typescript
const response = await fetch(`${config.apiUrl}/api/v1/chat/?t=${Date.now()}`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        ...(config.apiKey && { 'X-API-Key': config.apiKey }),
    },
    body: JSON.stringify({
        message,
        session_id: config.sessionId,
        user_id: config.userId || 'demo_user',
        stream: config.stream !== false,
        model_config: config.modelConfig || {
            max_tokens: 8000,
        },
    }),
});
```

**✅ 格式完全一致！**

## 🔍 SSE 事件类型对比

### HTML 中处理的事件类型：

1. **delta** - 内容增量
   ```javascript
   if (data.type === 'delta') {
       fullResponse += data.content;
   }
   ```

2. **tool_calls** - 工具调用
   ```javascript
   if (data.type === 'tool_calls') {
       const toolNames = data.tool_calls.map(tc => tc.function?.name).join(', ');
   }
   ```

3. **workflow events** - 工作流事件
   ```javascript
   if (data.level === 'graph') {
       // workflow_started, node_started, node_finished, route_decision, workflow_complete
   } else if (data.level === 'node') {
       // thinking_phase, tool_call_pending, tool_call_executing, tool_call_result
   }
   ```

### React 实现（已确认匹配）：

位置：`client/src/hooks/use-sse-stream.ts`

```typescript
// 内容增量
if (event.type === 'delta' && 'content' in event) {
    callbacks.onDelta?.(content);
}
// 工作流事件
else if ('level' in event && (event.level === 'graph' || event.level === 'node')) {
    callbacks.onWorkflowEvent?.(event as WorkflowEvent);
}
// 工具调用
else if (event.type === 'tool_calls' && 'tool_calls' in event) {
    callbacks.onToolCalls?.(event.tool_calls);
}
```

**✅ 事件处理完全匹配！**

## 🧪 测试步骤

### 1. 启动开发服务器

```bash
cd client
pnpm dev
```

### 2. 访问测试页面

```
http://localhost:3000/ai-workflow
```

### 3. 使用示例问题测试

页面提供了以下快速测试按钮：

- 🌤️ **查询天气** - "帮我查询今天的天气"
- 🔍 **搜索信息** - "帮我搜索 React 19 的新特性"
- 🔢 **计算数字** - "计算 123 * 456 等于多少？"
- ⏰ **获取时间** - "获取当前时间"

### 4. 查看控制台日志

**请求日志：**
```
📤 发送 API 请求: {
  url: "http://127.0.0.1:8000/api/v1/chat/?t=1234567890",
  method: "POST",
  headers: { ... },
  body: { message, session_id, user_id, stream, model_config }
}
```

**响应日志：**
```
✅ API 响应成功，开始处理流式数据
```

**工作流事件日志：**
```
🔄 工作流事件 [graph]: workflow_started {...}
🔄 工作流事件 [graph]: node_started {...}
```

**工具调用日志：**
```
🔧 工具调用: get_weather, search_web [...]
```

## 🐛 常见问题排查

### 问题 1：没有工具调用事件

**可能原因：**

1. **后端没有配置工具**
   - 检查后端是否正确配置了 tools
   - 确认 LLM 模型支持 function calling

2. **问题不够明确**
   - 使用页面提供的示例问题测试
   - 问题应明确需要工具辅助（如查询、计算、搜索）

3. **LLM 模型决策**
   - 模型可能认为不需要工具就能回答
   - 尝试更具体的问题，如"查询北京今天的天气"

### 问题 2：API 调用失败

**排查步骤：**

1. **检查后端服务**
   ```bash
   # 确认后端运行在 http://127.0.0.1:8000
   curl http://127.0.0.1:8000/health
   ```

2. **检查环境变量**
   ```typescript
   // client/src/app/ai-workflow/page.tsx
   const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://127.0.0.1:8000';
   const API_KEY = process.env.NEXT_PUBLIC_API_KEY || 'dev-test-key-123';
   ```

3. **查看网络请求**
   - 打开浏览器开发者工具 > Network
   - 查看 API 请求的状态码和响应
   - 确认请求头和请求体格式

### 问题 3：SSE 流式响应异常

**排查步骤：**

1. **检查 Content-Type**
   - 响应头应包含 `Content-Type: text/event-stream`

2. **检查事件格式**
   - 每行应以 `data:` 开头
   - JSON 格式应正确
   - 查看控制台是否有解析错误

3. **检查网络连接**
   - SSE 需要保持长连接
   - 检查是否有代理或防火墙干扰

## 📊 调试信息收集

如果仍然无法工作，请收集以下信息：

1. **浏览器控制台日志**
   - 包含所有 📤、✅、🔄、🔧 标记的日志
   - 包含任何错误信息

2. **Network 面板信息**
   - API 请求的完整内容（Request/Response Headers + Payload）
   - 响应的 Preview/Response 内容

3. **后端日志**
   - 后端是否接收到请求
   - 是否触发了工具调用
   - 是否正确发送了 SSE 事件

## 🎯 验证清单

- [ ] 后端服务正常运行 (http://127.0.0.1:8000)
- [ ] 前端开发服务器运行 (http://localhost:3000)
- [ ] 访问 /ai-workflow 页面
- [ ] 能看到输入框和快速示例按钮
- [ ] 点击示例按钮发送消息
- [ ] 控制台显示 "📤 发送 API 请求"
- [ ] 控制台显示 "✅ API 响应成功"
- [ ] 能看到流式文本输出
- [ ] 工作流时间线显示（如果后端支持）
- [ ] 工具调用显示（如果后端触发）

## 🔧 高级调试

### 启用详细日志

在 `use-sse-stream.ts` 中添加更多日志：

```typescript
// 在 processStream 中添加
for (const line of lines) {
    console.log('📥 原始 SSE 行:', line);
    const event = parseSSELine(line);
    if (event) {
        console.log('📦 解析后事件:', event);
        handleSSEEvent(event, callbacks);
    }
}
```

### 对比 HTML 和 React 的网络请求

1. 打开 `chat_demo.html` 发送相同问题
2. 打开 React 页面发送相同问题
3. 在 Network 面板对比两个请求的：
   - Request URL
   - Request Headers
   - Request Payload
   - Response Headers
   - Response Content

## 📝 总结

我们的实现与 HTML 示例**完全一致**：

✅ API 调用格式匹配  
✅ 请求头格式匹配  
✅ 请求体结构匹配  
✅ SSE 事件处理匹配  
✅ 工具调用处理匹配  

如果工具调用仍然不工作，问题很可能在于：

1. **后端配置** - 工具没有正确注册或启用
2. **模型决策** - LLM 认为不需要工具
3. **问题表述** - 问题不够明确触发工具使用

建议使用页面提供的示例问题进行测试，这些问题应该能够触发常见的工具调用。
